pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
joblib>=1.3
fastapi>=0.100
uvicorn>=0.23
pydantic>=2.0

# REQUIRED, not optional: the committed models/quantile_models.joblib was trained
# with the XGBoost backend, so unpickling it fails with ModuleNotFoundError
# without this installed. (README's "sklearn backend" note is out of date.)
xgboost>=2.0

# optional - only needed for SHAP explainability work
# shap>=0.44

# Image enhancer (src/image_enhance.py). Headless OpenCV: no GUI bindings
# needed on a server, and it avoids pulling in Qt.
opencv-python-headless>=4.8
pillow>=10.0
